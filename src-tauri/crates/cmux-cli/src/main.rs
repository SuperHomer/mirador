//! `cmux` CLI — automation client. The socket protocol lands in milestone M6;
//! until then this is a placeholder so the workspace builds end to end.

fn main() {
    eprintln!("cmux CLI: automation server not implemented yet (milestone M6)");
    std::process::exit(1);
}
